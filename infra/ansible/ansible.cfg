


[defaults]
inventory = inventory.ini
remote_user = ubuntu
private_key_file = ~/.ssh/capstone-phoenix-key
host_key_checking = False
roles_path = roles

[privilege_escalation]
become = True
become_method = sudo
become_user = root